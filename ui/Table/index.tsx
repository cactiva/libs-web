import * as React from "react";

export default (props: {
  columnMode?: "manual";
  children: any;
  onRowClick?: (e) => void,
  mobileCol?: number | ((any) => any);
}) => {
  return <div></div>;
};
